<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dane z bazy danych</title>
    <style>
        .center {
            width: 100vw;
            text-align: center;
        }

        .m-left {
            margin-left: 7vw;
        }

        .m-right {
            margin-right: 7vw;
        }

        .padding {
            padding: 0.25rem;
        }
    </style>
</head>
<body>
<h3 class="center">Zawartość kolekcji pracownicy</h3>
<div style="width: 100vw; display: flex; justify-content: space-between; align-items: center;">
    <div class="m-left">
        <form action="/wyswietl" method="get">
            <h4>Filtrowanie</h4>
            <label class="padding">
                <input type="text" placeholder="Imię" name="imie">
            </label>
            <label class="padding">
                <input type="text" placeholder="Nazwisko" name="nazwisko">
            </label>
            <label class="padding">
                <input type="text" placeholder="Stanowisko" name="stanowisko">
            </label>
            <button>Filtruj</button>
            <button form="wyczysc_form">Wyczyść filtr</button>
        </form>
        <form id="wyczysc_form" action="/wyswietl" method="get"></form>
    </div>
    <div class="m-right">
        <form action="/dodaj" method="get">
            <button>Dodaj nowego pracownika</button>
        </form>
    </div>
</div>
<table class="center">
    <tr>
        <th>Imię</th>
        <th>Nazwisko</th>
        <th>Stanowisko</th>
        <th>Akcja</th>
    </tr>
    <% for(let i = 0; i < a.length; i++) { %>
        <tr>
            <td>
                <%= a[i].imie; %>
            </td>
            <td>
                <%= a[i].nazwisko; %>
            </td>
            <td>
                <%= a[i].stanowisko; %>
            </td>
            <td>
                <form action="/aktualizacja/<%= a[i]._id; %>" method="get">
                    <button>Edytuj</button>
                </form>
                <form action="/usuwanie/<%= a[i]._id; %>" method="post">
                    <button>Usuń</button>
                </form>
            </td>
        </tr>
    <% } %>
</table>
</body>
</html>